name: manual trigger workflow

on:
  workflow_dispatch: 

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: print welcome information
        run: echo "Hello, this is a manual trigger workflow！"

